# Upwork Toolkit Source

This is the reconstructed source project for the unpacked extension in `../upwork-toolkit-pro`.

Current status:

- Phase 2 scaffold is present.
- Dependencies are declared but not installed.
- The app is intentionally a placeholder shell until Phase 3+ rebuild state, UI, and GraphQL behavior.

Useful docs:

- `../docs/MIGRATION_PLAN.md`
- `../docs/APP_MAP.md`
- `../docs/GRAPHQL_NOTES.md`
- `../docs/RECONSTRUCTION_LOG.md`

Planned commands after dependencies are installed:

```powershell
npm install
npm run dev
npm run build
npm run typecheck
```
